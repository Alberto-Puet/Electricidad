/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://raulpuet.com', // o tu dominio sin tilde si usás uno alternativo
  generateRobotsTxt: true,
  sitemapSize: 7000,
};
